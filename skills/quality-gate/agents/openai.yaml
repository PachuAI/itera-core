interface:
  display_name: "Quality Gate"
  short_description: "Repo audit con score y severidades"
  default_prompt: "Use $quality-gate to run a structured quality audit, score the repo, and report findings by severity."

policy:
  allow_implicit_invocation: true
